<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cz.drdla.matej.fivethingsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TabLayoutActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PageOne"></activity>
        <activity android:name=".PageTwo"></activity>
        <activity android:name=".PageThree"></activity>
        <activity android:name=".PageFour"></activity>
        <activity android:name=".PageFive"></activity>
    </application>

</manifest>
